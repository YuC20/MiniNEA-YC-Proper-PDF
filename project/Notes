31/01/2026
#1 Problem:
  How to seperate player movement speed and platform movement speed
  
Solution: Solved in commit "e82300d89fd255911464bc4baa7cdd69f58be642"
  Looped through list "wait = [0, 1, 2, 3, 4, 5]" each time everything was updated and only allowed platforms to move when "wait[current] == 0"  

01/02/2026
#1 Problem:
  Platform does not go up after reaching lowest point

Solution: Solved in commit "4bac97be03d99a03e5b6ab0ae1a81c8a1d0ca7e4"
  added logic to show direction platform should go. "plat_direction = 1 or -1"


03/02/2026
#1 Problem:
  Platform moves before player "dc2696bd33eb4ea8367743f04aa4b4bb86fa3e12"

Solution: Solved in commmit Solved in commmit 
  Reorder logic
  1. Calculate Platform Position
  2. Calculate Player position


04/02/2026
#0.5 Problem:
  Platform only moves the player down. Inefficient to copy and paste repeated code for the player to move back up with the platform. Code would also make it difficult to add new y coordinates for the player. 

Solution: Solved in commmit "dc2696bd33eb4ea8367743f04aa4b4bb86fa3e12"
  Added logic which moves the player by keeping it within the confines the platform meaning its y coordinates are dependant on the platforms y coordinates
  


#1 Problem:
  Player randomly teleports into random positions when moving up and down with the platform.
  This happened due to me using 2 ways of making the player move with the platform. One was keeping the player with the platform sprite. 

  "if abs(player_values["x"] - plat_values["x"]) < 5: 
        player_values["y"] = plat_values["y"] - 8"

  and manually updating the players vertical height 

  "if player_on_plat_x and player_on_plat_y and plat_moving_down:
    if ycoordPlayer > 0:
        ycoordPlayer -= 1
        player_values["y"] = playery[ycoordPlayer]"  <----- This line


Solution: Solved in commmit  "7290381829c361668bfdfe4a09d82b6a87da60cc"
  I only make the keeping the player on the platform logic move the sprite and only use the manual updating of the players vertical height to keep track of which platform level the player is currently on. 

05/02/2026
#0.5 Problem:
  Using same set of variables for moving both the platforms does not allow for individual movement in different directions for the platforms

Solution: Solved in commit "28a386a08f474e947f1b01830d2a9bba56ff4e3f" and "1f67bf0344e7e9179419d89abff48aa6d77d5ce1"
  Created seperate logic and variables for both platforms. *Note*This could be made more efficient at a later date

13/02/2026

#1 Problem:
  Player is teleporting up to platform above it

Solution: Added logic to see if the platform is on same elvation as the player 
          if abs(player_values["x"] - plat_values["x"]) < 5 and abs((player_values["y"] - plat_values["y"]) < 12): 

  
